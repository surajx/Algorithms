def LCS(x,y):
  """
  Computes the length of the Longest common subsequence in x and y
  Returns Int"""
  #1. Identify the optimal substructure
    #1.1 When calculating the Longest common subsequence we can see that if a prefix
      # of X, X_i=<X1 X2 X3...Xi> is a common subsequence then
  #2. Identify overlapping sub-problems
  #3. Porpose a theorem for the given problem
  #4. Obtain the final solution by
